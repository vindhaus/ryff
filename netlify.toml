[build]
  publish = "public"

[functions]
  node_bundler = "esbuild"
  included_files = ["lib/**", "netlify/wfos.json"]

# If your account supports Scheduled Functions, uncomment:
# [functions."refreshAFD"]
#   schedule = "*/10 * * * *"  # every 10 minutes
